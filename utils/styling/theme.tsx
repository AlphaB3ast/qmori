import { createTheme } from "@mui/material/styles";

// APPLY GUIDE FROM https://dev-yakuza.posstree.com/en/react/nextjs/mui/

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ff8e88",
    },
  },
});
